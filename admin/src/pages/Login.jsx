import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {useAdminAuth} from '../context/AdminAuthContext';export default function Login(){const [form,setForm]=useState({email:'',password:''});const [err,setErr]=useState('');const {login}=useAdminAuth();const nav=useNavigate();async function submit(e){e.preventDefault();try{await login(form.email,form.password);nav('/')}catch(e){setErr(e.message)}}return <div className="admin-login"><form onSubmit={submit}><img className="admin-login-logo" src="/sbn-kirana-logo.svg" alt="SBN KIRANA"/><small>SECURE ADMIN PORTAL</small><h1>Welcome back</h1><p>Sign in to manage your digital grocery store.</p>{err&&<div className="admin-alert">{err}</div>}<label>Admin email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label><button>Enter Dashboard →</button></form></div>}
