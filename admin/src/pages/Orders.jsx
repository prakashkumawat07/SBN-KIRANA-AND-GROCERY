import {useEffect,useState} from 'react';import {adminApi} from '../api';const statuses=['Placed','Confirmed','Packed','Shipped','Delivered','Cancelled'];export default function Orders(){const [orders,setOrders]=useState([]);const load=()=>adminApi('/admin/orders').then(setOrders);useEffect(()=>{load()},[]);async function change(id,status){await adminApi(`/admin/orders/${id}/status`,{method:'PATCH',body:JSON.stringify({status})});load()}return <><div className="admin-title"><div><small>ORDER MANAGEMENT</small><h1>Orders & Fulfilment</h1><p>Track payment method, PayLater dues and fulfilment status.</p></div><span>{orders.length} total</span></div><section className="panel"><div className="table-wrap"><table><thead><tr><th>Order</th><th>Customer</th><th>Items</th><th>Total</th><th>Payment</th><th>Payment Status</th><th>Fulfilment</th></tr></thead><tbody>{orders.map(o=><tr key={o._id}><td>#{o._id.slice(-8)}</td><td><b>{o.user?.name}</b><small className="table-sub">{o.user?.email}</small></td><td>{o.items.reduce((s,i)=>s+i.quantity,0)}</td><td>₹{o.total}</td><td><span className={o.paymentMethod==='PAYLATER'?'badge credit-badge':'badge'}>{o.paymentMethod}</span>{o.payLaterDueDate&&<small className="table-sub">Due {new Date(o.payLaterDueDate).toLocaleDateString('en-IN')}</small>}</td><td>{o.paymentStatus||'Pending'}</td><td><select value={o.status} onChange={e=>change(o._id,e.target.value)}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td></tr>)}</tbody></table></div></section></>}
