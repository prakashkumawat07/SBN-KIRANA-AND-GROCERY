import {Router} from 'express';import {protect,adminOnly} from '../middleware/auth.js';import {dashboard,products,createProduct,updateProduct,deleteProduct,orders,updateOrderStatus,customers,messages} from '../controllers/adminController.js';const r=Router();r.use(protect,adminOnly);r.get('/dashboard',dashboard);r.get('/products',products);r.post('/products',createProduct);r.put('/products/:id',updateProduct);r.delete('/products/:id',deleteProduct);r.get('/orders',orders);r.patch('/orders/:id/status',updateOrderStatus);r.get('/customers',customers);r.get('/messages',messages);export default r;
