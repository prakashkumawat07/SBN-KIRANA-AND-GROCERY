import Order from '../models/Order.js';import Product from '../models/Product.js';export async function createOrder(req,res,next){try{const {items,shippingAddress,paymentMethod}=req.body;if(!items?.length)return res.status(400).json({message:'Cart is empty'});const orderItems=[];let subtotal=0;for(const item of items){const p=await Product.findById(item.product);const q=Number(item.quantity);if(!p||q<1||p.stock<q)return res.status(400).json({message:`Invalid quantity for ${p?.name||'product'}`});orderItems.push({product:p._id,name:p.name,image:p.image,price:p.price,quantity:q});subtotal+=p.price*q;p.stock-=q;await p.save()}const deliveryFee=subtotal>=499?0:49;const order=await Order.create({user:req.user._id,items:orderItems,shippingAddress,paymentMethod,subtotal,deliveryFee,total:subtotal+deliveryFee});res.status(201).json(order)}catch(e){next(e)}}export async function myOrders(req,res,next){try{res.json(await Order.find({user:req.user._id}).sort({createdAt:-1}))}catch(e){next(e)}}
