import Message from '../models/Message.js';export async function createMessage(req,res,next){try{const {name,email,phone,subject,message}=req.body;if(!name||!email||!subject||!message)return res.status(400).json({message:'Please complete all required fields'});const saved=await Message.create({name,email,phone,subject,message});res.status(201).json({message:'Message received',id:saved._id})}catch(e){next(e)}}
