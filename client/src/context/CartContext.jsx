import {createContext,useContext,useEffect,useMemo,useState} from 'react';const CartContext=createContext();export const useCart=()=>useContext(CartContext);
export function CartProvider({children}){const [items,setItems]=useState(()=>JSON.parse(localStorage.getItem('sbn_cart')||'[]'));useEffect(()=>localStorage.setItem('sbn_cart',JSON.stringify(items)),[items]);const addToCart=p=>setItems(x=>{const f=x.find(i=>i._id===p._id);return f?x.map(i=>i._id===p._id?{...i,qty:Math.min(i.qty+1,p.stock)}:i):[...x,{...p,qty:1}]});const updateQty=(id,qty)=>setItems(x=>x.map(i=>i._id===id?{...i,qty:Math.max(1,Math.min(Number(qty)||1,i.stock))}:i));const remove=id=>setItems(x=>x.filter(i=>i._id!==id));const clear=()=>setItems([]);const subtotal=useMemo(()=>items.reduce((s,i)=>s+i.price*i.qty,0),[items]);const count=items.reduce((s,i)=>s+i.qty,0);return <CartContext.Provider value={{items,addToCart,updateQty,remove,clear,subtotal,count}}>{children}</CartContext.Provider>}
