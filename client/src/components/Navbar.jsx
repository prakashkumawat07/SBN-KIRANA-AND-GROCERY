import { Link, NavLink } from 'react-router-dom';
import { useAuth } from '../context/AuthContext';import { useCart } from '../context/CartContext';
export default function Navbar(){const {user,logout}=useAuth();const {count}=useCart();return <><div className="topbar">Free delivery on orders above ₹499</div><header><Link className="brand" to="/"><span>SBN</span><b>KIRANA & GROCERY</b></Link><nav><NavLink to="/">Home</NavLink><NavLink to="/products">Shop</NavLink><NavLink to="/contact">Contact</NavLink>{user&&<NavLink to="/orders">My Orders</NavLink>}</nav><div className="actions">{user?<><span>Hi, {user.name.split(' ')[0]}</span><button className="link-btn" onClick={logout}>Logout</button></>:<Link to="/login">Login</Link>}<Link className="cart-link" to="/cart">Cart <b>{count}</b></Link></div></header></>}
