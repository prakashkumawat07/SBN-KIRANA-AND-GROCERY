import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {api} from '../api';import {useCart} from '../context/CartContext';
export default function Checkout(){const {items,subtotal,clear}=useCart();const nav=useNavigate();const [form,setForm]=useState({fullName:'',phone:'',address:'',city:'',state:'Rajasthan',pincode:'',paymentMethod:'COD'});const [error,setError]=useState('');const delivery=subtotal>=499?0:49;async function submit(e){e.preventDefault();if(!items.length)return;try{await api('/orders',{method:'POST',body:JSON.stringify({items:items.map(i=>({product:i._id,quantity:i.qty})),shippingAddress:form,paymentMethod:form.paymentMethod})});clear();nav('/orders')}catch(e){setError(e.message)}}return <main className="section"><div className="page-title"><span className="eyebrow">Almost there</span><h1>Checkout</h1></div><div className="checkout-layout"><form className="form-card" onSubmit={submit}><h2>Delivery Details</h2>{error&&<div className="alert">{error}</div>}<div className="form-grid">{['fullName','phone','address','city','state','pincode'].map(k=><label key={k}>{k.replace(/([A-Z])/g,' $1')}<input required value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}</div><label>Payment Method<select value={form.paymentMethod} onChange={e=>setForm({...form,paymentMethod:e.target.value})}><option value="COD">Cash on Delivery</option><option value="UPI">UPI (demo)</option></select></label><button>Place Order · ₹{subtotal+delivery}</button></form><aside className="summary"><h2>Your Order</h2>{items.map(i=><p key={i._id}><span>{i.name} × {i.qty}</span><b>₹{i.price*i.qty}</b></p>)}<hr/><p><span>Delivery</span><b>{delivery?'₹49':'FREE'}</b></p><p className="total"><span>Total</span><b>₹{subtotal+delivery}</b></p></aside></div></main>}
