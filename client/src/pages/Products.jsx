import {useEffect,useState} from 'react';import {api} from '../api';import ProductCard from '../components/ProductCard';
const cats=['All','Fruits & Vegetables','Staples','Dairy','Snacks','Beverages','Household'];export default function Products(){const [products,setProducts]=useState([]);const [search,setSearch]=useState('');const [category,setCategory]=useState('All');useEffect(()=>{const q=new URLSearchParams();if(search)q.set('search',search);if(category!=='All')q.set('category',category);api(`/products?${q}`).then(setProducts).catch(()=>{})},[search,category]);return <main className="section"><div className="page-title"><span className="eyebrow">Shop smart</span><h1>All Products</h1><p>Find your everyday essentials in seconds.</p></div><div className="shop-tools"><input placeholder="Search atta, milk, rice..." value={search} onChange={e=>setSearch(e.target.value)}/><div className="chips">{cats.map(c=><button className={category===c?'active':''} onClick={()=>setCategory(c)} key={c}>{c}</button>)}</div></div><div className="product-grid">{products.map(p=><ProductCard key={p._id} product={p}/>)}</div>{!products.length&&<div className="empty">No products found.</div>}</main>}
