import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';
export default function Register(){const [form,setForm]=useState({name:'',email:'',phone:'',password:''});const [error,setError]=useState('');const {register}=useAuth();const nav=useNavigate();async function submit(e){e.preventDefault();try{await register(form);nav('/')}catch(e){setError(e.message)}}return <main className="auth-page"><form className="auth-card" onSubmit={submit}><div className="eyebrow">Join SBN</div><h1>Create account</h1>{error&&<div className="alert">{error}</div>}{['name','email','phone','password'].map(k=><label key={k}>{k[0].toUpperCase()+k.slice(1)}<input required={k!=='phone'} type={k==='password'?'password':k==='email'?'email':'text'} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<button>Create Account</button><p>Already registered? <Link to="/login">Sign in</Link></p></form></main>}
