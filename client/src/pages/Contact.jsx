import {useState} from 'react';import {api} from '../api';
export default function Contact(){const [form,setForm]=useState({name:'',email:'',phone:'',subject:'',message:''});const [note,setNote]=useState('');async function submit(e){e.preventDefault();try{await api('/contact',{method:'POST',body:JSON.stringify(form)});setNote('Thanks! Your message has been sent.');setForm({name:'',email:'',phone:'',subject:'',message:''})}catch(e){setNote(e.message)}}return <main className="section"><div className="contact-layout"><div className="contact-copy"><span className="eyebrow">We are here to help</span><h1>Contact SBN</h1><p>Have a product question, order concern or feedback? Send us a message.</p><div className="contact-box"><b>Customer Support</b><span>Mon–Sat · 9:00 AM–8:00 PM</span><span>support@sbnkirana.com</span><span>+91 90000 00000</span></div></div><form className="form-card" onSubmit={submit}><h2>Send a message</h2>{note&&<div className="notice">{note}</div>}{['name','email','phone','subject'].map(k=><label key={k}>{k}<input required={k!=='phone'} value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<label>Message<textarea rows="5" required value={form.message} onChange={e=>setForm({...form,message:e.target.value})}/></label><button>Send Message</button></form></div></main>}
